---
phase: 01-limites-mensais-por-categoria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/FinanceBot.Domain/Entities/LimiteCategoria.cs
  - src/FinanceBot.Application/Abstractions/ILimiteCategoriaRepository.cs
  - src/FinanceBot.Infrastructure/Repositories/LimiteCategoriaRepository.cs
  - src/FinanceBot.Infrastructure/Persistence/FinanceBotDbContext.cs
  - src/FinanceBot.Infrastructure/DependencyInjection/InfrastructureServiceCollectionExtensions.cs
autonomous: true
requirements:
  - LIMITE-01
  - LIMITE-02
  - LIMITE-05

must_haves:
  truths:
    - "Tabela `limite_categorias` existe no banco com colunas `id`, `categoria`, `valor`"
    - "Uma linha por categoria (unique constraint em `categoria`)"
    - "`ILimiteCategoriaRepository` está registrado no container DI"
    - "Migration gerada e aplicada sem erros"
  artifacts:
    - path: "src/FinanceBot.Domain/Entities/LimiteCategoria.cs"
      provides: "Entidade de domínio para limite por categoria"
      contains: "public sealed class LimiteCategoria"
    - path: "src/FinanceBot.Application/Abstractions/ILimiteCategoriaRepository.cs"
      provides: "Contrato do repositório"
      exports: ["GetByCategoriaAsync", "UpsertAsync", "DeleteAsync"]
    - path: "src/FinanceBot.Infrastructure/Repositories/LimiteCategoriaRepository.cs"
      provides: "Implementação EF Core do repositório"
      contains: "public sealed class LimiteCategoriaRepository"
    - path: "src/FinanceBot.Infrastructure/Persistence/FinanceBotDbContext.cs"
      provides: "DbSet e configuração da tabela"
      contains: "DbSet<LimiteCategoria>"
  key_links:
    - from: "FinanceBotDbContext"
      to: "limite_categorias (PostgreSQL)"
      via: "EF Core migration"
      pattern: "ToTable\\(\"limite_categorias\"\\)"
    - from: "InfrastructureServiceCollectionExtensions"
      to: "ILimiteCategoriaRepository"
      via: "AddScoped"
      pattern: "AddScoped<ILimiteCategoriaRepository, LimiteCategoriaRepository>"
---

<objective>
Implementar a camada de dados para limites mensais por categoria: entidade de domínio, interface do repositório, implementação EF Core, configuração do DbContext e migration.

Purpose: Fornecer a fundação de persistência que o plan 01-02 (command handling) irá consumir.
Output: Tabela `limite_categorias` no banco, repositório funcional, DI registrado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Padrões existentes que este plan deve espelhar. -->

De src/FinanceBot.Domain/Entities/Compra.cs:
```csharp
public sealed class Compra
{
    public int Id { get; set; }
    public double Valor { get; set; }
    public string Descricao { get; set; } = string.Empty;
    public Categoria Categoria { get; set; }
    public DateTime Data { get; set; }
}
```

De src/FinanceBot.Application/Abstractions/ICompraRepository.cs:
```csharp
public interface ICompraRepository
{
    Task AddAsync(Compra compra, CancellationToken cancellationToken);
    Task<IReadOnlyList<Compra>> ListByPeriodAsync(DateTime periodStartUtc, DateTime periodEndUtc, CancellationToken cancellationToken);
    Task<Compra?> FindByIdAsync(int compraId, CancellationToken cancellationToken);
    Task DeleteAsync(Compra compra, CancellationToken cancellationToken);
}
```

De src/FinanceBot.Infrastructure/Persistence/FinanceBotDbContext.cs:
```csharp
public sealed class FinanceBotDbContext : DbContext
{
    public DbSet<Compra> Compras => Set<Compra>();
    protected override void OnModelCreating(ModelBuilder modelBuilder)
    {
        base.OnModelCreating(modelBuilder);
        ConfigureCompra(modelBuilder.Entity<Compra>());
    }
    private static void ConfigureCompra(EntityTypeBuilder<Compra> compraConfiguration)
    {
        compraConfiguration.ToTable("compras");
        compraConfiguration.HasKey(compra => compra.Id);
        compraConfiguration.Property(compra => compra.Categoria)
            .HasColumnName("categoria")
            .HasConversion<string>()
            .HasMaxLength(50)
            .IsRequired();
    }
}
```

De src/FinanceBot.Infrastructure/DependencyInjection/InfrastructureServiceCollectionExtensions.cs:
```csharp
services.AddScoped<ICompraRepository, CompraRepository>();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar entidade, interface e repositório</name>
  <files>
    src/FinanceBot.Domain/Entities/LimiteCategoria.cs
    src/FinanceBot.Application/Abstractions/ILimiteCategoriaRepository.cs
    src/FinanceBot.Infrastructure/Repositories/LimiteCategoriaRepository.cs
  </files>
  <read_first>
    - src/FinanceBot.Domain/Entities/Compra.cs (padrão de entidade: sealed class, auto-props)
    - src/FinanceBot.Application/Abstractions/ICompraRepository.cs (padrão de interface: async + CancellationToken)
    - src/FinanceBot.Infrastructure/Repositories/CompraRepository.cs (padrão de repositório: injeta DbContext, EF Core direto)
    - src/FinanceBot.Domain/Enums/Categoria.cs (enum usado como chave)
  </read_first>
  <action>
**1. Criar `src/FinanceBot.Domain/Entities/LimiteCategoria.cs`:**

```csharp
using FinanceBot.Domain.Enums;

namespace FinanceBot.Domain.Entities;

public sealed class LimiteCategoria
{
    public int Id { get; set; }

    public Categoria Categoria { get; set; }

    public double Valor { get; set; }
}
```

**2. Criar `src/FinanceBot.Application/Abstractions/ILimiteCategoriaRepository.cs`:**

```csharp
using FinanceBot.Domain.Entities;
using FinanceBot.Domain.Enums;

namespace FinanceBot.Application.Abstractions;

public interface ILimiteCategoriaRepository
{
    Task<LimiteCategoria?> GetByCategoriaAsync(Categoria categoria, CancellationToken cancellationToken);

    Task UpsertAsync(LimiteCategoria limiteCategoria, CancellationToken cancellationToken);

    Task DeleteAsync(LimiteCategoria limiteCategoria, CancellationToken cancellationToken);
}
```

Semântica dos métodos:
- `GetByCategoriaAsync`: retorna null se não houver limite definido para a categoria.
- `UpsertAsync`: cria se não existir, atualiza se já existir (via `AddAsync` + `SaveChangesAsync` para novo; `SaveChangesAsync` após mutar `Valor` para existente). Implementar como: busca existente por categoria; se null → Add; senão → muta Valor. Então SaveChanges.
- `DeleteAsync`: remove a entidade já carregada e SaveChanges.

**3. Criar `src/FinanceBot.Infrastructure/Repositories/LimiteCategoriaRepository.cs`:**

```csharp
using FinanceBot.Application.Abstractions;
using FinanceBot.Domain.Entities;
using FinanceBot.Domain.Enums;
using FinanceBot.Infrastructure.Persistence;
using Microsoft.EntityFrameworkCore;

namespace FinanceBot.Infrastructure.Repositories;

public sealed class LimiteCategoriaRepository : ILimiteCategoriaRepository
{
    private readonly FinanceBotDbContext _financeBotDbContext;

    public LimiteCategoriaRepository(FinanceBotDbContext financeBotDbContext)
    {
        _financeBotDbContext = financeBotDbContext;
    }

    public async Task<LimiteCategoria?> GetByCategoriaAsync(
        Categoria categoria,
        CancellationToken cancellationToken)
    {
        return await _financeBotDbContext.LimiteCategorias
            .AsNoTracking()
            .FirstOrDefaultAsync(l => l.Categoria == categoria, cancellationToken);
    }

    public async Task UpsertAsync(
        LimiteCategoria limiteCategoria,
        CancellationToken cancellationToken)
    {
        LimiteCategoria? existing = await _financeBotDbContext.LimiteCategorias
            .FirstOrDefaultAsync(l => l.Categoria == limiteCategoria.Categoria, cancellationToken);

        if (existing is null)
        {
            await _financeBotDbContext.LimiteCategorias.AddAsync(limiteCategoria, cancellationToken);
        }
        else
        {
            existing.Valor = limiteCategoria.Valor;
        }

        await _financeBotDbContext.SaveChangesAsync(cancellationToken);
    }

    public async Task DeleteAsync(
        LimiteCategoria limiteCategoria,
        CancellationToken cancellationToken)
    {
        _financeBotDbContext.LimiteCategorias.Remove(limiteCategoria);
        await _financeBotDbContext.SaveChangesAsync(cancellationToken);
    }
}
```

Nota: `GetByCategoriaAsync` usa `AsNoTracking()` pois é para leitura (progresso após /compra). `UpsertAsync` NÃO usa AsNoTracking — precisa rastrear `existing` para mutação.
  </action>
  <verify>
    <automated>cd "D:/dev/telegrambot/financas-pessoais-telegram" && dotnet build src/FinanceBot.Domain/FinanceBot.Domain.csproj --no-restore -q 2>&1 | tail -5 && dotnet build src/FinanceBot.Application/FinanceBot.Application.csproj --no-restore -q 2>&1 | tail -5 && dotnet build src/FinanceBot.Infrastructure/FinanceBot.Infrastructure.csproj --no-restore -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "public sealed class LimiteCategoria" src/FinanceBot.Domain/Entities/LimiteCategoria.cs` retorna resultado
    - `grep -r "public Categoria Categoria { get; set; }" src/FinanceBot.Domain/Entities/LimiteCategoria.cs` retorna resultado
    - `grep -r "public double Valor { get; set; }" src/FinanceBot.Domain/Entities/LimiteCategoria.cs` retorna resultado
    - `grep -r "Task<LimiteCategoria?> GetByCategoriaAsync" src/FinanceBot.Application/Abstractions/ILimiteCategoriaRepository.cs` retorna resultado
    - `grep -r "Task UpsertAsync" src/FinanceBot.Application/Abstractions/ILimiteCategoriaRepository.cs` retorna resultado
    - `grep -r "Task DeleteAsync" src/FinanceBot.Application/Abstractions/ILimiteCategoriaRepository.cs` retorna resultado
    - `grep -r "public sealed class LimiteCategoriaRepository : ILimiteCategoriaRepository" src/FinanceBot.Infrastructure/Repositories/LimiteCategoriaRepository.cs` retorna resultado
    - Build dos três projetos sem erros
  </acceptance_criteria>
  <done>Três arquivos criados, compilando sem erros.</done>
</task>

<task type="auto">
  <name>Task 2: Configurar DbContext, registrar DI e aplicar migration</name>
  <files>
    src/FinanceBot.Infrastructure/Persistence/FinanceBotDbContext.cs
    src/FinanceBot.Infrastructure/DependencyInjection/InfrastructureServiceCollectionExtensions.cs
  </files>
  <read_first>
    - src/FinanceBot.Infrastructure/Persistence/FinanceBotDbContext.cs (padrão de ConfigureX inline, HasConversion&lt;string&gt;, snake_case)
    - src/FinanceBot.Infrastructure/DependencyInjection/InfrastructureServiceCollectionExtensions.cs (padrão AddScoped)
  </read_first>
  <action>
**1. Modificar `FinanceBotDbContext.cs`:**

Adicionar `DbSet<LimiteCategoria>` e chamar `ConfigureLimiteCategoria` no `OnModelCreating`.

Adicionar ao topo do arquivo (using):
```csharp
// já existe: using FinanceBot.Domain.Entities;
// já existe: using Microsoft.EntityFrameworkCore;
// já existe: using Microsoft.EntityFrameworkCore.Metadata.Builders;
```
Nenhum using novo necessário — `LimiteCategoria` está no mesmo namespace que `Compra`.

Adicionar property após `public DbSet<Compra> Compras => Set<Compra>();`:
```csharp
public DbSet<LimiteCategoria> LimiteCategorias => Set<LimiteCategoria>();
```

Adicionar chamada no `OnModelCreating` após `ConfigureCompra(...)`:
```csharp
ConfigureLimiteCategoria(modelBuilder.Entity<LimiteCategoria>());
```

Adicionar método privado estático após `ConfigureCompra`:
```csharp
private static void ConfigureLimiteCategoria(EntityTypeBuilder<LimiteCategoria> cfg)
{
    cfg.ToTable("limite_categorias");
    cfg.HasKey(l => l.Id);

    cfg.Property(l => l.Id)
        .HasColumnName("id");

    cfg.Property(l => l.Categoria)
        .HasColumnName("categoria")
        .HasConversion<string>()
        .HasMaxLength(50)
        .IsRequired();

    cfg.HasIndex(l => l.Categoria)
        .IsUnique();

    cfg.Property(l => l.Valor)
        .HasColumnName("valor")
        .IsRequired();
}
```

**2. Modificar `InfrastructureServiceCollectionExtensions.cs`:**

Adicionar após a linha `services.AddScoped<ICompraRepository, CompraRepository>();`:
```csharp
services.AddScoped<ILimiteCategoriaRepository, LimiteCategoriaRepository>();
```

Adicionar using se necessário (o namespace já deve estar acessível via existing usings do arquivo).

**3. Criar e aplicar migration:**

Executar na raiz do projeto (`D:/dev/telegrambot/financas-pessoais-telegram`):

```bash
dotnet ef migrations add AddLimiteCategoria \
  --project src/FinanceBot.Infrastructure \
  --startup-project src/FinanceBot.Api

dotnet ef database update \
  --project src/FinanceBot.Infrastructure \
  --startup-project src/FinanceBot.Api
```

Se o `dotnet ef` não estiver disponível globalmente, usar:
```bash
dotnet tool run dotnet-ef migrations add AddLimiteCategoria \
  --project src/FinanceBot.Infrastructure \
  --startup-project src/FinanceBot.Api
```

Verificar que o arquivo de migration gerado contém `CreateTable(name: "limite_categorias"` e `column: "categoria"` com unique index.
  </action>
  <verify>
    <automated>cd "D:/dev/telegrambot/financas-pessoais-telegram" && dotnet build src/FinanceBot.Infrastructure/FinanceBot.Infrastructure.csproj --no-restore -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DbSet<LimiteCategoria>" src/FinanceBot.Infrastructure/Persistence/FinanceBotDbContext.cs` retorna resultado
    - `grep "ConfigureLimiteCategoria" src/FinanceBot.Infrastructure/Persistence/FinanceBotDbContext.cs` retorna resultado
    - `grep "ToTable(\"limite_categorias\")" src/FinanceBot.Infrastructure/Persistence/FinanceBotDbContext.cs` retorna resultado
    - `grep "HasIndex.*IsUnique" src/FinanceBot.Infrastructure/Persistence/FinanceBotDbContext.cs` retorna resultado
    - `grep "AddScoped<ILimiteCategoriaRepository, LimiteCategoriaRepository>" src/FinanceBot.Infrastructure/DependencyInjection/InfrastructureServiceCollectionExtensions.cs` retorna resultado
    - Arquivo de migration em `src/FinanceBot.Infrastructure/Migrations/*AddLimiteCategoria.cs` existe
    - Migration contém `CreateTable(name: "limite_categorias"`
    - `dotnet ef database update` executa sem erros
    - Build sem erros
  </acceptance_criteria>
  <done>DbContext configurado, DI registrado, migration criada e aplicada ao banco.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Este plan não expõe endpoints | Camada puramente de dados — sem input externo direto neste plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | LimiteCategoriaRepository.UpsertAsync | mitigate | EF Core parameterized queries — nenhuma interpolação de string em SQL. O campo `Categoria` é enum (não string livre); `Valor` é double. Sem SQL injection possível. |
| T-01-01-02 | Information Disclosure | FinanceBotDbContext | accept | Banco local, uso pessoal, sem multi-usuário. Sem dados sensíveis além de valores de gastos pessoais. |
| T-01-01-03 | Denial of Service | Migration (AddLimiteCategoria) | accept | Migration executada uma vez em deploy — não é surface de ataque em runtime. |
</threat_model>

<verification>
Após concluir o plan:
1. `dotnet build src/ -q` — zero erros de compilação
2. Arquivo de migration existe em `src/FinanceBot.Infrastructure/Migrations/`
3. Migration contém `CreateTable(name: "limite_categorias"` com colunas `id`, `categoria`, `valor`
4. `grep -r "AddScoped<ILimiteCategoriaRepository" src/` retorna resultado em InfrastructureServiceCollectionExtensions.cs
</verification>

<success_criteria>
- `LimiteCategoria` entity existe com `Id`, `Categoria` (enum), `Valor` (double)
- `ILimiteCategoriaRepository` define `GetByCategoriaAsync`, `UpsertAsync`, `DeleteAsync` com CancellationToken
- `LimiteCategoriaRepository` implementa a interface usando EF Core
- `FinanceBotDbContext` tem `DbSet<LimiteCategoria>` e tabela `limite_categorias` configurada com unique index em `categoria`
- `ILimiteCategoriaRepository` registrado via `AddScoped` em `InfrastructureServiceCollectionExtensions`
- Migration `AddLimiteCategoria` criada e banco atualizado
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-limites-mensais-por-categoria/01-01-SUMMARY.md` com:
- Arquivos criados/modificados
- Assinatura exata da interface `ILimiteCategoriaRepository`
- Nome do arquivo de migration gerado
- Qualquer desvio do plano
</output>
