---
phase: 01-limites-mensais-por-categoria
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/FinanceBot.Application/UseCases/TelegramCommandRouter.cs
  - tests/FinanceBot.UnitTests/Application/TelegramCommandRouterTests.cs
autonomous: true
requirements:
  - LIMITE-01
  - LIMITE-02
  - LIMITE-03
  - LIMITE-04
  - LIMITE-05

must_haves:
  truths:
    - "Usuário envia `/limite Mercado 800` → bot responde 'Limite de Mercado definido: R$ 800,00'"
    - "Usuário envia `/limite Mercado 0` → bot responde 'Limite de Mercado removido'"
    - "Após `/compra 340, Pao, Mercado` com limite R$ 800 e gasto acumulado R$ 340 → resposta inclui 'Mercado: R$ 340,00 de R$ 800,00 (42%)'"
    - "Após `/compra 50, Extra, Mercado` com limite R$ 800 e gasto acumulado R$ 820 → resposta inclui '⚠ Limite de Mercado ultrapassado: R$ 820,00 de R$ 800,00'"
    - "Categoria sem limite → resposta do /compra sem linha de progresso (comportamento original preservado)"
    - "Todos os testes unitários passam"
  artifacts:
    - path: "src/FinanceBot.Application/UseCases/TelegramCommandRouter.cs"
      provides: "Handler /limite e progresso no /compra"
      contains: "HandleLimiteAsync"
    - path: "tests/FinanceBot.UnitTests/Application/TelegramCommandRouterTests.cs"
      provides: "Cobertura de testes para /limite e progresso"
      contains: "InMemoryLimiteCategoriaRepository"
  key_links:
    - from: "TelegramCommandRouter.HandleCompraAsync"
      to: "ILimiteCategoriaRepository.GetByCategoriaAsync"
      via: "await após registrar a compra"
      pattern: "GetByCategoriaAsync\\(categoria"
    - from: "TelegramCommandRouter.HandleLimiteAsync"
      to: "ILimiteCategoriaRepository.UpsertAsync / DeleteAsync"
      via: "condicional Valor > 0"
      pattern: "UpsertAsync|DeleteAsync"
---

<objective>
Implementar o comando `/limite` e o feedback de progresso/alerta no `/compra`, modificando `TelegramCommandRouter` para consumir `ILimiteCategoriaRepository`. Cobrir com testes unitários.

Purpose: Entregar ao usuário as funcionalidades LIMITE-01 a LIMITE-05 — o coração deste milestone.
Output: TelegramCommandRouter atualizado com handler /limite e progresso no /compra; testes unitários cobrindo os fluxos principais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-limites-mensais-por-categoria/01-01-SUMMARY.md

<interfaces>
<!-- Contratos criados no plan 01-01 que este plan consome. -->

De src/FinanceBot.Application/Abstractions/ILimiteCategoriaRepository.cs:
```csharp
public interface ILimiteCategoriaRepository
{
    Task<LimiteCategoria?> GetByCategoriaAsync(Categoria categoria, CancellationToken cancellationToken);
    Task UpsertAsync(LimiteCategoria limiteCategoria, CancellationToken cancellationToken);
    Task DeleteAsync(LimiteCategoria limiteCategoria, CancellationToken cancellationToken);
}
```

De src/FinanceBot.Domain/Entities/LimiteCategoria.cs:
```csharp
public sealed class LimiteCategoria
{
    public int Id { get; set; }
    public Categoria Categoria { get; set; }
    public double Valor { get; set; }
}
```

De src/FinanceBot.Application/UseCases/TelegramCommandRouter.cs (estado atual):
```csharp
// Construtor existente:
public TelegramCommandRouter(ICompraRepository compraRepository)
{
    _compraRepository = compraRepository;
}

// Método existente que será modificado:
private async Task<string> HandleCompraAsync(string messageText, DateTimeOffset messageDateUtc, CancellationToken cancellationToken)
// ... persiste compra, retorna: $"Compra registrada: R$ {formattedValor} - {categoriaDisplayName}"

// Infraestrutura existente reutilizável:
// - TryParseCategoria(string categoriaText, out Categoria categoria) — já normaliza texto
// - GetCategoriaDisplayName(Categoria categoria) — já retorna string display
// - CultureInfo PtBrCulture — já existe como campo estático
```

Formato exato das respostas a implementar (LIMITE-03 e LIMITE-04):
- Progresso (gasto <= limite): `"Mercado: R$ 340,00 de R$ 800,00 (42%)"`
- Alerta (gasto > limite): `"⚠ Limite de Mercado ultrapassado: R$ 820,00 de R$ 800,00"`

A linha de progresso/alerta é ADICIONADA à resposta do /compra, não substitui.
Resposta completa quando há limite:
```
Compra registrada: R$ 50,00 - Mercado
Mercado: R$ 50,00 de R$ 800,00 (6%)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atualizar TelegramCommandRouter com /limite e progresso no /compra</name>
  <files>
    src/FinanceBot.Application/UseCases/TelegramCommandRouter.cs
  </files>
  <read_first>
    - src/FinanceBot.Application/UseCases/TelegramCommandRouter.cs (arquivo completo — estado atual)
    - src/FinanceBot.Application/Abstractions/ILimiteCategoriaRepository.cs (contrato criado no plan 01-01)
    - src/FinanceBot.Domain/Entities/LimiteCategoria.cs (entidade criada no plan 01-01)
  </read_first>
  <behavior>
    - `/limite Mercado 800` → chama UpsertAsync com Valor=800.0 → retorna "Limite de Mercado definido: R$ 800,00"
    - `/limite Mercado 0` → chama DeleteAsync (se existir) → retorna "Limite de Mercado removido"
    - `/limite Mercado 0` sem limite definido → retorna "Limite de Mercado removido" (idempotente)
    - `/limite categoriaInvalida 100` → retorna erro de categoria inválida
    - `/limite Mercado abc` → retorna "Valor inválido. Use um numero positivo ou 0 para remover."
    - `/limite Mercado -50` → retorna erro (valor negativo não permitido)
    - `/compra 340, Pao, Mercado` com limite R$ 800 e gastos anteriores R$ 0 → resposta inclui "Mercado: R$ 340,00 de R$ 800,00 (42%)"
    - `/compra 50, Extra, Mercado` com limite R$ 800 e gastos acumulados R$ 820 → resposta inclui "⚠ Limite de Mercado ultrapassado: R$ 820,00 de R$ 800,00"
    - `/compra 50, Cafe, Mercado` sem limite → resposta é apenas "Compra registrada: R$ 50,00 - Mercado" (sem linha de progresso)
  </behavior>
  <action>
**Modificações em `TelegramCommandRouter.cs`:**

**1. Adicionar using:**
```csharp
using FinanceBot.Domain.Entities;  // já existe — sem mudança
// Adicionar se ainda não existir:
```
O namespace `FinanceBot.Application.Abstractions` já está referenciado via `ICompraRepository`.

**2. Adicionar Regex estático:**
Após a linha de `DeletarCommandRegex`, adicionar:
```csharp
private static readonly Regex LimiteCommandRegex = new Regex(
    "^/limite(?:@[A-Za-z0-9_]+)?\\s+(\\S+)\\s+([0-9]+(?:[.,][0-9]{1,2})?)\\s*$",
    RegexOptions.Compiled | RegexOptions.CultureInvariant | RegexOptions.IgnoreCase);
```

**3. Adicionar campo ao construtor:**
```csharp
private readonly ICompraRepository _compraRepository;
private readonly ILimiteCategoriaRepository _limiteCategoriaRepository;

public TelegramCommandRouter(
    ICompraRepository compraRepository,
    ILimiteCategoriaRepository limiteCategoriaRepository)
{
    _compraRepository = compraRepository;
    _limiteCategoriaRepository = limiteCategoriaRepository;
}
```

**4. Adicionar roteamento em `RouteAsync`:**
Adicionar após o bloco `/deletar` e antes do bloco `/start`:
```csharp
if (normalizedMessageText.StartsWith("/limite", StringComparison.OrdinalIgnoreCase))
{
    return await HandleLimiteAsync(normalizedMessageText, cancellationToken);
}
```

**5. Adicionar método `HandleLimiteAsync`:**
```csharp
private async Task<string> HandleLimiteAsync(
    string messageText,
    CancellationToken cancellationToken)
{
    Match commandMatch = LimiteCommandRegex.Match(messageText);
    if (!commandMatch.Success)
    {
        return "Uso correto: /limite <CATEGORIA> <VALOR>. Exemplo: /limite Mercado 800";
    }

    string categoriaText = commandMatch.Groups[1].Value.Trim();
    string valorText = commandMatch.Groups[2].Value;

    bool categoriaReconhecida = TryParseCategoria(categoriaText, out Categoria categoria);
    if (!categoriaReconhecida)
    {
        return $"Categoria '{categoriaText}' inexistente.\n\nCategorias disponiveis:\n" +
            BuildCategoriasValidas();
    }

    string normalizedValorText = valorText.Replace(',', '.');
    bool parsedValor = double.TryParse(
        normalizedValorText,
        NumberStyles.AllowDecimalPoint,
        CultureInfo.InvariantCulture,
        out double valor);

    if (!parsedValor || valor < 0)
    {
        return "Valor invalido. Use um numero positivo ou 0 para remover o limite.";
    }

    string categoriaDisplayName = GetCategoriaDisplayName(categoria);

    if (valor == 0)
    {
        LimiteCategoria? existing = await _limiteCategoriaRepository
            .GetByCategoriaAsync(categoria, cancellationToken);

        if (existing is not null)
        {
            await _limiteCategoriaRepository.DeleteAsync(existing, cancellationToken);
        }

        return $"Limite de {categoriaDisplayName} removido";
    }

    LimiteCategoria limiteCategoria = new LimiteCategoria
    {
        Categoria = categoria,
        Valor = valor
    };

    await _limiteCategoriaRepository.UpsertAsync(limiteCategoria, cancellationToken);

    string formattedValor = valor.ToString("N2", PtBrCulture);
    return $"Limite de {categoriaDisplayName} definido: R$ {formattedValor}";
}
```

**6. Modificar `HandleCompraAsync`:**

Após o `await _compraRepository.AddAsync(compra, cancellationToken);`, substituir o bloco de retorno por:

```csharp
await _compraRepository.AddAsync(compra, cancellationToken);

string formattedValor = valor.ToString("N2", PtBrCulture);
string categoriaDisplayName = GetCategoriaDisplayName(categoria);
string baseResponse = $"Compra registrada: R$ {formattedValor} - {categoriaDisplayName}";

LimiteCategoria? limite = await _limiteCategoriaRepository
    .GetByCategoriaAsync(categoria, cancellationToken);

if (limite is null)
{
    return baseResponse;
}

DateTime periodStartUtc = new DateTime(
    compraDateUtc.Year,
    compraDateUtc.Month,
    1,
    0,
    0,
    0,
    DateTimeKind.Utc);
DateTime periodEndUtc = periodStartUtc.AddMonths(1);

IReadOnlyList<Compra> comprasDoMes = await _compraRepository.ListByPeriodAsync(
    periodStartUtc,
    periodEndUtc,
    cancellationToken);

double gastoAcumulado = 0;
foreach (Compra c in comprasDoMes)
{
    if (c.Categoria == categoria)
    {
        gastoAcumulado += c.Valor;
    }
}

string formattedGasto = gastoAcumulado.ToString("N2", PtBrCulture);
string formattedLimite = limite.Valor.ToString("N2", PtBrCulture);

if (gastoAcumulado > limite.Valor)
{
    return $"{baseResponse}\n⚠ Limite de {categoriaDisplayName} ultrapassado: R$ {formattedGasto} de R$ {formattedLimite}";
}

int percentual = (int)Math.Round(gastoAcumulado / limite.Valor * 100);
return $"{baseResponse}\n{categoriaDisplayName}: R$ {formattedGasto} de R$ {formattedLimite} ({percentual}%)";
```

Remover as duas linhas de retorno que existiam antes neste método (a linha `string formattedValor = valor.ToString(...)` e o `return $"Compra registrada:..."`) — elas são substituídas pelo bloco acima.

**Atenção:** `compraDateUtc` já está disponível no escopo de `HandleCompraAsync` (linha existente `DateTime compraDateUtc = messageDateUtc.UtcDateTime;`). Não redeclará-la.
  </action>
  <verify>
    <automated>cd "D:/dev/telegrambot/financas-pessoais-telegram" && dotnet build src/FinanceBot.Application/FinanceBot.Application.csproj --no-restore -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "LimiteCommandRegex" src/FinanceBot.Application/UseCases/TelegramCommandRouter.cs` retorna resultado
    - `grep "HandleLimiteAsync" src/FinanceBot.Application/UseCases/TelegramCommandRouter.cs` retorna resultado (pelo menos 2 ocorrências: definição + chamada)
    - `grep "ILimiteCategoriaRepository _limiteCategoriaRepository" src/FinanceBot.Application/UseCases/TelegramCommandRouter.cs` retorna resultado
    - `grep "GetByCategoriaAsync" src/FinanceBot.Application/UseCases/TelegramCommandRouter.cs` retorna resultado (pelo menos 2: em HandleLimite e HandleCompra)
    - `grep "ultrapassado" src/FinanceBot.Application/UseCases/TelegramCommandRouter.cs` retorna resultado
    - `grep "⚠ Limite de" src/FinanceBot.Application/UseCases/TelegramCommandRouter.cs` retorna resultado
    - Build do projeto Application sem erros
  </acceptance_criteria>
  <done>
    TelegramCommandRouter compilando com handler /limite e progresso no /compra.
    Respostas corretas: "Limite de X definido: R$ Y", "Limite de X removido", linha de progresso, linha de alerta com ⚠.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes unitários para /limite e progresso no /compra</name>
  <files>
    tests/FinanceBot.UnitTests/Application/TelegramCommandRouterTests.cs
  </files>
  <read_first>
    - tests/FinanceBot.UnitTests/Application/TelegramCommandRouterTests.cs (arquivo completo — padrão InMemory existente)
    - src/FinanceBot.Application/Abstractions/ILimiteCategoriaRepository.cs (interface a ser fakeada)
    - src/FinanceBot.Domain/Entities/LimiteCategoria.cs (entidade usada pelo fake)
  </read_first>
  <behavior>
    - Teste: `/limite Mercado 800` → resposta contém "Limite de Mercado definido: R$ 800,00"
    - Teste: `/limite Mercado 0` sem limite prévo → resposta contém "Limite de Mercado removido"
    - Teste: `/limite Mercado 0` com limite prévio de R$800 → resposta contém "Limite de Mercado removido"; repositório fica vazio
    - Teste: `/limite categoriaInexistente 100` → resposta contém "inexistente" e "Categorias disponiveis"
    - Teste: `/compra 340, Pao, Mercado` com limite R$800 e sem compras anteriores → resposta contém "Mercado: R$ 340,00 de R$ 800,00 (42%)"
    - Teste: `/compra 50, Extra, Mercado` com limite R$800 e compras anteriores de R$770 → gasto acumulado R$820 → resposta contém "⚠ Limite de Mercado ultrapassado: R$ 820,00 de R$ 800,00"
    - Teste: `/compra 50, Cafe, Mercado` sem limite → resposta é "Compra registrada: R$ 50,00 - Mercado" e NÃO contém "de R$" nem "⚠"
  </behavior>
  <action>
**Adicionar ao arquivo `TelegramCommandRouterTests.cs`:**

**1. Adicionar usings necessários no topo do arquivo (após os existentes):**
```csharp
using FinanceBot.Domain.Entities;
using FinanceBot.Domain.Enums;
```
(Verificar se já existem — se sim, não duplicar.)

**2. Adicionar classe fake `InMemoryLimiteCategoriaRepository` dentro do namespace, após a classe `InMemoryCompraRepository` existente:**

```csharp
private sealed class InMemoryLimiteCategoriaRepository : ILimiteCategoriaRepository
{
    private readonly List<LimiteCategoria> _limites = new List<LimiteCategoria>();
    private int _nextId = 1;

    public Task<LimiteCategoria?> GetByCategoriaAsync(
        Categoria categoria,
        CancellationToken cancellationToken)
    {
        LimiteCategoria? limite = _limites.FirstOrDefault(l => l.Categoria == categoria);
        return Task.FromResult(limite);
    }

    public Task UpsertAsync(
        LimiteCategoria limiteCategoria,
        CancellationToken cancellationToken)
    {
        LimiteCategoria? existing = _limites.FirstOrDefault(l => l.Categoria == limiteCategoria.Categoria);
        if (existing is null)
        {
            limiteCategoria.Id = _nextId++;
            _limites.Add(limiteCategoria);
        }
        else
        {
            existing.Valor = limiteCategoria.Valor;
        }

        return Task.CompletedTask;
    }

    public Task DeleteAsync(
        LimiteCategoria limiteCategoria,
        CancellationToken cancellationToken)
    {
        _limites.Remove(limiteCategoria);
        return Task.CompletedTask;
    }
}
```

**3. Atualizar os dois testes existentes** para usar o novo construtor de `TelegramCommandRouter` (que agora requer dois parâmetros). Substituir:
```csharp
// ANTES:
TelegramCommandRouter telegramCommandRouter = new TelegramCommandRouter(inMemoryCompraRepository);

// DEPOIS:
TelegramCommandRouter telegramCommandRouter = new TelegramCommandRouter(
    inMemoryCompraRepository,
    new InMemoryLimiteCategoriaRepository());
```

**4. Adicionar novos testes (como métodos [Fact] na classe `TelegramCommandRouterTests`):**

```csharp
[Fact]
public async Task RouteAsync_WithLimiteDefinido_ReturnsConfirmacaoComValor()
{
    InMemoryLimiteCategoriaRepository limiteRepository = new InMemoryLimiteCategoriaRepository();
    TelegramCommandRouter router = new TelegramCommandRouter(
        new InMemoryCompraRepository(),
        limiteRepository);

    string response = await router.RouteAsync(
        "/limite Mercado 800",
        DateTimeOffset.UtcNow,
        CancellationToken.None);

    Assert.Contains("Limite de Mercado definido", response);
    Assert.Contains("R$ 800,00", response);
}

[Fact]
public async Task RouteAsync_WithLimiteZero_RemoveLimite()
{
    InMemoryLimiteCategoriaRepository limiteRepository = new InMemoryLimiteCategoriaRepository();
    TelegramCommandRouter router = new TelegramCommandRouter(
        new InMemoryCompraRepository(),
        limiteRepository);

    // Define primeiro
    await router.RouteAsync("/limite Mercado 800", DateTimeOffset.UtcNow, CancellationToken.None);

    // Remove
    string response = await router.RouteAsync(
        "/limite Mercado 0",
        DateTimeOffset.UtcNow,
        CancellationToken.None);

    Assert.Contains("Limite de Mercado removido", response);
    LimiteCategoria? limite = await limiteRepository.GetByCategoriaAsync(
        Categoria.Mercado,
        CancellationToken.None);
    Assert.Null(limite);
}

[Fact]
public async Task RouteAsync_WithLimiteCategoriaInvalida_RetornaErro()
{
    TelegramCommandRouter router = new TelegramCommandRouter(
        new InMemoryCompraRepository(),
        new InMemoryLimiteCategoriaRepository());

    string response = await router.RouteAsync(
        "/limite categoriaInexistente 100",
        DateTimeOffset.UtcNow,
        CancellationToken.None);

    Assert.Contains("inexistente", response);
    Assert.Contains("Categorias disponiveis", response);
}

[Fact]
public async Task RouteAsync_ComCompraELimite_ExibeProgressoNaResposta()
{
    InMemoryCompraRepository compraRepository = new InMemoryCompraRepository();
    InMemoryLimiteCategoriaRepository limiteRepository = new InMemoryLimiteCategoriaRepository();
    TelegramCommandRouter router = new TelegramCommandRouter(compraRepository, limiteRepository);

    // Define limite
    await router.RouteAsync("/limite Mercado 800", DateTimeOffset.UtcNow, CancellationToken.None);

    // Registra compra de R$ 340
    string response = await router.RouteAsync(
        "/compra 340, Pao, Mercado",
        DateTimeOffset.UtcNow,
        CancellationToken.None);

    Assert.Contains("Compra registrada", response);
    Assert.Contains("Mercado: R$ 340,00 de R$ 800,00", response);
    Assert.Contains("42%", response);
}

[Fact]
public async Task RouteAsync_ComCompraQueUltrapassaLimite_ExibeAlerta()
{
    InMemoryCompraRepository compraRepository = new InMemoryCompraRepository();
    InMemoryLimiteCategoriaRepository limiteRepository = new InMemoryLimiteCategoriaRepository();
    TelegramCommandRouter router = new TelegramCommandRouter(compraRepository, limiteRepository);

    DateTimeOffset now = DateTimeOffset.UtcNow;

    // Define limite R$ 800
    await router.RouteAsync("/limite Mercado 800", now, CancellationToken.None);

    // Registra compra prévia de R$ 770 (coloca no In-Memory diretamente)
    await router.RouteAsync("/compra 770, Compras anteriores, Mercado", now, CancellationToken.None);

    // Registra compra que ultrapassa (770 + 50 = 820 > 800)
    string response = await router.RouteAsync(
        "/compra 50, Extra, Mercado",
        now,
        CancellationToken.None);

    Assert.Contains("Compra registrada", response);
    Assert.Contains("⚠ Limite de Mercado ultrapassado", response);
    Assert.Contains("R$ 820,00 de R$ 800,00", response);
}

[Fact]
public async Task RouteAsync_ComCompraSemLimite_NaoExibeProgresso()
{
    TelegramCommandRouter router = new TelegramCommandRouter(
        new InMemoryCompraRepository(),
        new InMemoryLimiteCategoriaRepository());

    string response = await router.RouteAsync(
        "/compra 50, Cafe, Mercado",
        DateTimeOffset.UtcNow,
        CancellationToken.None);

    Assert.Contains("Compra registrada: R$ 50,00 - Mercado", response);
    Assert.DoesNotContain("de R$", response);
    Assert.DoesNotContain("⚠", response);
}
```

**Nota importante sobre o teste de ultrapassagem:**
O `InMemoryCompraRepository.ListByPeriodAsync` existente retorna `Compras` sem filtrar por período (retorna tudo). Isso é adequado para testes unitários — o comportamento é idêntico para compras do mesmo mês (que é o caso de todos os testes). NÃO alterar a implementação fake existente.
  </action>
  <verify>
    <automated>cd "D:/dev/telegrambot/financas-pessoais-telegram" && dotnet test tests/FinanceBot.UnitTests/FinanceBot.UnitTests.csproj --no-build -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep "InMemoryLimiteCategoriaRepository" tests/FinanceBot.UnitTests/Application/TelegramCommandRouterTests.cs` retorna resultado
    - `grep "RouteAsync_WithLimiteDefinido_ReturnsConfirmacaoComValor" tests/FinanceBot.UnitTests/Application/TelegramCommandRouterTests.cs` retorna resultado
    - `grep "RouteAsync_ComCompraELimite_ExibeProgressoNaResposta" tests/FinanceBot.UnitTests/Application/TelegramCommandRouterTests.cs` retorna resultado
    - `grep "RouteAsync_ComCompraQueUltrapassaLimite_ExibeAlerta" tests/FinanceBot.UnitTests/Application/TelegramCommandRouterTests.cs` retorna resultado
    - `grep "RouteAsync_ComCompraSemLimite_NaoExibeProgresso" tests/FinanceBot.UnitTests/Application/TelegramCommandRouterTests.cs` retorna resultado
    - `dotnet test tests/FinanceBot.UnitTests/` — todos os testes passam (0 falhas)
    - Os dois testes existentes (`RouteAsync_WithCompraAndUnknownCategory` e `RouteAsync_WithCompraMentionAndUnknownCategory`) continuam passando
  </acceptance_criteria>
  <done>
    Todos os testes unitários passando. Cobertura: definição de limite, remoção, categoria inválida, progresso dentro do limite, alerta de ultrapassagem, comportamento sem limite.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram → WebhookEndpoint → TelegramCommandRouter | messageText é input não confiável do usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | HandleLimiteAsync — parsing de valor | mitigate | `double.TryParse` com `NumberStyles.AllowDecimalPoint` + validação `valor < 0` rejeita strings arbitrárias. Regex `LimiteCommandRegex` restringe o formato aceito a `[0-9]+([.,][0-9]{1,2})?` — sem injeção possível. |
| T-01-02-02 | Spoofing | TryParseCategoria | accept | Lookup em `Dictionary<string, Categoria>` com chave normalizada — sem acesso a dados de outras categorias. Sem multi-usuário: não há outro usuário para impersonar. |
| T-01-02-03 | Information Disclosure | Resposta de /compra | accept | Resposta expõe apenas dados do próprio usuário (bot pessoal, sem multi-usuário). |
| T-01-02-04 | Denial of Service | ListByPeriodAsync chamado no /compra quando há limite | accept | Chamada EF Core parametrizada com filtro de período (um mês). Volume de dados controlado (uso pessoal). Sem vetor de amplificação externa. |
</threat_model>

<verification>
Após concluir o plan:
1. `dotnet build src/ -q` — zero erros de compilação
2. `dotnet test tests/FinanceBot.UnitTests/ -q` — todos os testes passam
3. Testar manualmente via Telegram (se bot disponível):
   - `/limite Mercado 800` → "Limite de Mercado definido: R$ 800,00"
   - `/compra 340, Pão, Mercado` → resposta inclui "Mercado: R$ 340,00 de R$ 800,00 (42%)"
   - `/compra 500, Supermercado, Mercado` → resposta inclui "⚠ Limite de Mercado ultrapassado: R$ 840,00 de R$ 800,00"
   - `/limite Mercado 0` → "Limite de Mercado removido"
   - `/compra 50, Café, Mercado` → sem linha de progresso
</verification>

<success_criteria>
- `/limite <CATEGORIA> <VALOR>` (LIMITE-01) → persiste via UpsertAsync e confirma com valor formatado
- `/limite <CATEGORIA> 0` (LIMITE-02) → remove via DeleteAsync e confirma
- Após /compra com limite, exibe "CATEGORIA: R$ X de R$ Y (Z%)" (LIMITE-03)
- Quando gasto > limite, exibe "⚠ Limite de CATEGORIA ultrapassado: R$ X de R$ Y" (LIMITE-04)
- Progresso calculado via ListByPeriodAsync do mês da compra — sem estado acumulado (LIMITE-05)
- Todos os testes unitários passam (0 falhas, 0 skips)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-limites-mensais-por-categoria/01-02-SUMMARY.md` com:
- Confirmação de que todos os testes passam
- Comportamento exato das respostas (strings)
- Qualquer desvio do plano
</output>
